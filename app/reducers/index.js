/* reducers */
// import reducers here

const defaultState = {
  // app state here
};

export default (state = defaultState, action) => {
  switch(action.type) {
    // cases here
    default:
      return state;
  }
};
